title: "UNLIMITED BANGSUWAN"
description: ": Ruang Pribadi Tanpa Batas ~"
url: "https://Sulbae.github.io"

markdown: kramdown
permalink: pretty
timezone: Asia/Jakarta

collections:
  projects:
    output: true
    permalink: /projects/:slug/ # URL pattern: /projects/water-potability-assessment/
    sort_by: date
    order: descending
  articles:
    output: true
    permalink: /articles/:slug/

defaults:
  - scope:
      path: ""
      type: projects
    values:
      layout: tabloid # Layout default
      published: true # Default published

  - scope:
      path: ""
      type: articles
    values:
      layout: tabloid # Layout default
      published: true # Default published

author:
  name: "Anggun Sulis Setyawan"
  alias: "Bang Suwan"
  role: "Entry-level Machine Learning Engineer"

readme_index:
  enabled: false

titles_from_headings:
  enabled: false
